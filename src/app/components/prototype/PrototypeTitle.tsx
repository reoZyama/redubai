import { Typography } from '@mui/material';

export default function PrototypeTitle() {
  return (
      <Typography
        variant="h3"
        sx={{
          color: 'black',
          fontFamily: 'Helvetica',
          fontWeight: 'bold',
        }}
      >
        Prototype
      </Typography>
  );
}
