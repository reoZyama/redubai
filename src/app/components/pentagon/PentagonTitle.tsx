import { Typography } from '@mui/material';

export default function PentagonTitle() {
  return (
    <Typography
      variant="h3"
      sx={{
        color: 'black',
        fontFamily: 'Helvetica',
        fontWeight: 'bold',
      }}
    >
      Pentagon
    </Typography>
  );
}
