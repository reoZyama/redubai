import { Typography } from '@mui/material';

export default function FormTitle() {
  return (
    <Typography
      variant="h3"
      sx={{
        color: 'black',
        fontFamily: 'Helvetica',
        fontWeight: 'bold',
      }}
    >
      Form
    </Typography>
  );
}
