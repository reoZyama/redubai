import { Typography } from '@mui/material';

export default function AggregationTitle() {
  return (
    <Typography
      variant="h3"
      sx={{
        color: 'black',
        fontFamily: 'Helvetica',
        fontWeight: 'bold',
      }}
    >
      Aggregation
    </Typography>
  );
}
