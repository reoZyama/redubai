import { Typography } from '@mui/material';

export default function FlowsTitle() {
  return (
    <Typography
      variant="h3"
      sx={{
        color: 'black',
        fontFamily: 'Helvetica',
        fontWeight: 'bold',
      }}
    >
      Flows
    </Typography>
  );
}
