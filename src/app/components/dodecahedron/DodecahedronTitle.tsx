import { Typography } from '@mui/material';

export default function DodecahedronTitle() {
  return (
    <Typography
      variant="h3"
      sx={{
        color: 'black',
        fontFamily: 'Helvetica',
        fontWeight: 'bold',
      }}
    >
      Dodecahedron
    </Typography>
  );
}
